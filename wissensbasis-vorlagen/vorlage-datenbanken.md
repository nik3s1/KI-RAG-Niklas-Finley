# Wissensbasis: SQL / Datenbanken (VORLAGE)

> Startgerüst. Nutzt euer Wissen aus den Datenbank-Projekten. Ergänzt und löscht
> die [TODO]-Hinweise.

## Daten abfragen (SELECT)

(Beispiel ausgefüllt:)

```sql
SELECT name, klasse
FROM schueler
WHERE klasse = 'ITA12';
```

## Daten einfügen / ändern / löschen

[TODO] INSERT, UPDATE, DELETE mit je einem Beispiel. Warnung zu DELETE ohne WHERE!

## Joins

[TODO] INNER JOIN vs. LEFT JOIN erklären, Beispiel mit zwei Tabellen.

## Normalisierung

[TODO] 1., 2., 3. Normalform in eigenen Worten + kurzes Beispiel.

## Häufige Fehler

[TODO] z.B. DELETE/UPDATE ohne WHERE, falsche Join-Bedingung, Datentyp-Probleme.
