# Wissensbasis: Linux / Shell (VORLAGE)

> Startgerüst. Nutzt euer Wissen (auch aus den Docker-Projekten). Ergänzt und
> löscht die [TODO]-Hinweise.

## Navigation im Dateisystem

(Beispiel ausgefüllt:)

```bash
pwd        # aktuelles Verzeichnis anzeigen
ls -la     # Inhalt inkl. versteckter Dateien
cd /etc    # Verzeichnis wechseln
```

## Dateien & Verzeichnisse

[TODO] cp, mv, rm, mkdir, touch – je kurz erklären. Warnung zu `rm -rf`!

## Dateirechte

[TODO] chmod, chown, das rwx-System erklären. Beispiel mit 755 / 644.

## Paketverwaltung

[TODO] apt update/install (Debian/Ubuntu). Wie installiert man ein Programm?

## Prozesse & System

[TODO] ps, top, kill, df, free – wofür braucht man sie?

## Häufige Fehler

[TODO] z.B. fehlende Rechte (sudo), Tippfehler bei Pfaden, `rm -rf` Unfälle.
